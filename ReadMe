The TIWVector4Value category adds support for 4 float component vectors to NSValue.

Implementation is in:

	NSValue+TIWVector4Extension.h
	NSValue+TIWVector4Extension.m

The LLVM CLang ext_vector_type language extension is required.
